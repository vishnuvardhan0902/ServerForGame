# In production, the frontend and backend are served from the same domain
# When deployed, this can be an empty string since both are at the same origin
REACT_APP_API_URL=https://serverforgame-thesacreddrop.onrender.com 